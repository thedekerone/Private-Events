<% if(session[:user_id]) %>

<h2>Welcome <%= User.find(session[:user_id]).username %> </h2>

<%else %>
<h2>Login</h2>
<%= form_with(url: "/sessions", method: "post") do %>
    <%= label_tag (:username) %>
    <%= text_field_tag (:username )%>
    <%= submit_tag ("Login") %>
<%end%>
<%end %>